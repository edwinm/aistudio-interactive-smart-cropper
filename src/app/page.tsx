import SmartCropClient from '@/components/smart-crop-client';

export default function SmartCropPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-4 md:p-8 animate-in fade-in duration-500">
      <SmartCropClient />
    </main>
  );
}
